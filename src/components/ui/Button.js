

const Button = ({ 
  children, 
  variant = 'primary', 
  size = 'medium', 
  disabled = false, 
  icon,
  onClick,
  type = 'button',
  className = '',
  ...props 
}) => {
  // Component implementation here
};

export default Button;